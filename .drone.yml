---
kind: pipeline
name: audit all modules

platform:
  os: linux
  arch: amd64

steps:
- name: build information
  image: node:lts
  commands:
  - node --version
  - npm --version

- name: install
  image: node:lts
  commands:
  - npm install
  - npm run bootstrap

- name: audit all
  image: node:lts
  commands:
  - lerna run audit

---
kind: pipeline
name: lint

platform:
  os: linux
  arch: amd64

steps:
- name: build information
  image: node:lts
  commands:
  - node --version
  - npm --version

- name: install
  image: node:lts
  commands:
  - npm install
  - npm run bootstrap

- name: lint all
  image: node:lts
  commands:
  - lerna run lint

---
kind: pipeline
name: core size and timing (node:lts)

platform:
  os: linux
  arch: amd64

steps:
- name: slow-deps
  image: node:lts
  commands:
  - npm install -g slow-deps
  - slow-deps

---
kind: pipeline
name: core unit tests (node:6)

platform:
  os: linux
  arch: amd64

steps:
- name: build information
  image: node:6
  commands:
  - node --version
  - npm --version

- name: install
  image: node:6
  commands:
  - npm install
  - npm run bootstrap

- name: unit tests
  image: node:6
  commands:
  - lerna run unit-test
  environment:
    BITGOJS_TEST_PASSWORD:
      from_secret: password

- name: upload coverage
  image: node:6
  commands:
  - npm install -g codecov
  - lerna run gen-coverage
  - lerna run upload-coverage -- -F unit
  environment:
    CODECOV_TOKEN:
      from_secret: codecov
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

---
kind: pipeline
name: core unit tests (node:8)

platform:
  os: linux
  arch: amd64

steps:
- name: build information
  image: node:8
  commands:
  - node --version
  - npm --version

- name: install
  image: node:8
  commands:
  - npm install
  - npm run bootstrap

- name: unit tests
  image: node:8
  commands:
  - lerna run unit-test
  environment:
    BITGOJS_TEST_PASSWORD:
      from_secret: password

- name: upload coverage
  image: node:8
  commands:
  - npm install -g codecov
  - lerna run gen-coverage
  - lerna run upload-coverage -- -F unit
  environment:
    CODECOV_TOKEN:
      from_secret: codecov
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

---
kind: pipeline
name: core unit tests (node:9)

platform:
  os: linux
  arch: amd64

steps:
- name: build information
  image: node:9
  commands:
  - node --version
  - npm --version

- name: install
  image: node:9
  commands:
  - npm install
  - npm run bootstrap

- name: unit tests
  image: node:9
  commands:
  - lerna run unit-test
  environment:
    BITGOJS_TEST_PASSWORD:
      from_secret: password

- name: upload coverage
  image: node:9
  commands:
  - npm install -g codecov
  - lerna run gen-coverage
  - lerna run upload-coverage -- -F unit
  environment:
    CODECOV_TOKEN:
      from_secret: codecov
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

---
kind: pipeline
name: core unit tests (node:10)

platform:
  os: linux
  arch: amd64

steps:
- name: build information
  image: node:10
  commands:
  - node --version
  - npm --version

- name: install
  image: node:10
  commands:
  - npm install
  - npm run bootstrap

- name: unit tests
  image: node:10
  commands:
  - lerna run unit-test
  environment:
    BITGOJS_TEST_PASSWORD:
      from_secret: password

- name: upload coverage
  image: node:10
  commands:
  - npm install -g codecov
  - lerna run gen-coverage
  - lerna run upload-coverage -- -F unit
  environment:
    CODECOV_TOKEN:
      from_secret: codecov
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

---
kind: pipeline
name: core unit tests (node:11)

platform:
  os: linux
  arch: amd64

steps:
- name: build information
  image: node:11
  commands:
  - node --version
  - npm --version

- name: install
  image: node:11
  commands:
  - npm install
  - npm run bootstrap

- name: unit tests
  image: node:11
  commands:
  - lerna run unit-test
  environment:
    BITGOJS_TEST_PASSWORD:
      from_secret: password

- name: upload coverage
  image: node:11
  commands:
  - npm install -g codecov
  - lerna run gen-coverage
  - lerna run upload-coverage -- -F unit
  environment:
    CODECOV_TOKEN:
      from_secret: codecov
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

---
kind: pipeline
name: core integration tests (node:lts)

platform:
  os: linux
  arch: amd64

steps:
- name: build information
  image: node:lts
  commands:
  - node --version
  - npm --version
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

- name: install
  image: node:lts
  commands:
  - npm install
  - npm run bootstrap
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

- name: integration tests
  image: node:lts
  commands:
  - lerna run integration-test
  environment:
    BITGOJS_TEST_PASSWORD:
      from_secret: password
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

- name: upload coverage
  image: node:lts
  commands:
  - npm install -g codecov
  - lerna run gen-coverage
  - lerna run upload-coverage -- -F integration
  environment:
    CODECOV_TOKEN:
      from_secret: codecov
  when:
    branch:
    - master
    - "rel/*"
    - prod/production

...
