{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "workspaces": [
    "modules/*"
  ],
  "scripts": {
    "generate-drone": "drone jsonnet --stream",
    "bootstrap": "lerna bootstrap",
    "audit": "lerna run --since master audit",
    "lint": "lerna run --since master lint",
    "unit-test": "lerna run --since master unit-test",
    "integration-test": "lerna run --since master integration-test"
  }
}
