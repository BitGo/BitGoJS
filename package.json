{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "workspaces": [
    "modules/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "generate-drone": "drone jsonnet --stream",
    "audit": "lerna run audit --since --canary --stream",
    "lint": "lerna run lint --since --canary --stream",
    "unit-test": "lerna run unit-test --since --canary --stream",
    "integration-test": "lerna run integration-test --since --canary --stream",
    "gen-coverage": "lerna run gen-coverage --since --canary --stream",
    "coverage": "lerna run upload-coverage --since --canary --stream --"
  }
}
