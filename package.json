{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "workspaces": [
    "modules/*"
  ],
  "scripts": {
    "generate-drone": "drone jsonnet --stream",
    "bootstrap": "lerna bootstrap",
    "audit": "lerna run --since audit",
    "lint": "lerna run --since lint",
    "unit-test": "lerna run --since unit-test",
    "integration-test": "lerna run --since integration-test"
  }
}
