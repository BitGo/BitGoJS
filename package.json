{
  "name": "@bitgo/statics",
  "version": "1.0.0",
  "description": "static configuration information for the bitgo platform",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "fmt": "prettier --write 'src/**/*.ts'",
    "check-fmt": "prettier --check 'src/**/*.ts'"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "lint-staged": "^8.1.5",
    "prettier": "^1.16.4",
    "typescript": "~3.4.0",
    "husky": "^1.3.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "**/*.ts": [
      "prettier --write",
      "git add"
    ]
  }
}
