{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "workspaces": [
    "modules/*"
  ],
  "scripts": {
    "generate-drone": "drone jsonnet --stream",
    "bootstrap": "lerna bootstrap",
    "audit": "lerna run audit",
    "lint": "lerna run lint",
    "unit-test": "lerna run unit-test",
    "integration-test": "lerna run integration-test",
    "lerna-run": "lerna run"
  }
}
