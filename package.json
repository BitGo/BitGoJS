{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "workspaces": [
    "modules/*"
  ],
  "scripts": {
    "generate-drone": "drone jsonnet --stream",
    "bootstrap": "lerna bootstrap",
    "audit": "lerna run audit --since",
    "lint": "lerna run lint --since",
    "unit-test": "lerna run unit-test --since",
    "integration-test": "lerna run integration-test --since",
    "report": "lerna exec gen-coverage --since",
    "coverage": "lerna exec upload-coverage --since"
  }
}
